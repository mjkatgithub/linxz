import { it, expect } from 'vitest'
import { isClient } from '~/stores/utils'

it('exposes isClient helper')

// it('exposes isClient helper', () => {
//   expect(typeof isClient()).toBe('boolean')
// })
